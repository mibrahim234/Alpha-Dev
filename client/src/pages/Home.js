import React from 'react';
// import { useQuery } from '@apollo/client';

const Home = () => {
    return (
        <main>
        <div>
            <h1>Hello World!</h1>
        </div>
    </main>
    )
  };



export default Home;
